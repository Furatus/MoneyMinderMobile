query CurrentUser{
    currentUser{
        id
        userName
        avatarUrl
        userGroups {
            group {
                id
                name
                groupImageUrl
            }
        }
        invitations {
            invitedAt
            group{
                id
                name
                groupImageUrl
            }
        }
    }
}