query CurrentUser{
    currentUser{
        id
        userName
        avatarUrl
        balance
        userGroups {
            group {
                id
                name
                groupImageUrl
            }
        }
        invitations {
            invitedAt
            group{
                id
                name
                groupImageUrl
            }
        }
    }
}