query CurrentUser{
    currentUser{
        id
        userName
        avatarUrl
        balance
        userGroups {
            group {
                id
                name
            }
        }

    }
}